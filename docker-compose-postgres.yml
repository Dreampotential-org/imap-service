version: '3.3'

services:
    db:
        image: postgis/postgis:13-3.2
        ports:
            - 5437:5432
        volumes:
            - /opt/ciimbmail:/var/lib/postgresql/data
        environment:
            POSTGRES_DB: postgres
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: awmyb
        networks:
            - backend

networks:
    backend:
        external:
            name: chiroposture_network

